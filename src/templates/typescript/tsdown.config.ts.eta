import { defineConfig } from "tsdown"

export default defineConfig({
  dts: true,
  entry: ["src/index.ts"],
  exports: {
    devExports: false,
  },
  format: "esm",
  inlineOnly: ["typesafe-i18n"],
  sourcemap: true,
})
